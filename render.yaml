services:
  - type: web
    name: antrea-netmonitor-backend
    env: go
    rootDir: backend
    buildCommand: go build -o bin/server ./cmd/server
    startCommand: PORT=8080 ./bin/server
    healthCheckPath: /health
    envVars:
      - key: CAPTURE_MODE
        value: mock
